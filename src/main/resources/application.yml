spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/AriPay?serverTimezone=UTC&characterEncoding=UTF-8
    password: phj@3473
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
  main:
    allow-bean-definition-overriding: true

server:
  servlet:
    context-path: /kiosk
